<%= form_with(model: question_set) do |form| %>
  <% if question_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_set.errors.count, "error") %> prohibited this question_set from being saved:</h2>

      <ul>
        <% question_set.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :score %>
    <%= form.text_field :score %>
  </div>

  <div class="field">
    <%= form.label :percent %>
    <%= form.text_field :percent %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
